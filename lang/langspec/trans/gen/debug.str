module gen/debug

imports

  libspoofax/stratego/debug

rules

  pipe-try-fail(|n) = debug(|$[Failed to apply [n] to:[" "]])
  pipe-try-debug(s|n) = s <+ (pipe-try-fail(|n); fail)
  pipe-filter-debug(s|n) = filter(s <+ pipe-try-debug(s|n))
