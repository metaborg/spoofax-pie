module gen/kotlin-ceres/build

imports
  
  libspoofax/stratego/debug
  
  gen/kotlin-ceres/core

rules

  generate-kotlin-ceres-file:
    (ast, _, _, file, _) -> (filename, result)
    with
      filename := <guarantee-extension(|"kt")> file
    ; result   := <p2c> ast
