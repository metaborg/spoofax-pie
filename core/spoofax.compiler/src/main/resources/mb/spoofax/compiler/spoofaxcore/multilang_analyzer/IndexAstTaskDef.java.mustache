package {{genIndexAstTaskDef.packageId}};

import mb.jsglr1.common.JSGLR1ParseException;
import mb.statix.multilang.pie.transform.SmlIndexAstTaskDef;
import mb.stratego.common.StrategoRuntime;

import javax.inject.Inject;
import javax.inject.Provider;

public class {{genIndexAstTaskDef.id}} extends SmlIndexAstTaskDef<JSGLR1ParseException> {

    public {{genIndexAstTaskDef.id}}(
        Provider<StrategoRuntime> strategoRuntimeProvider,
        Function<Supplier<String>, IStrategoTerm> astSupplier
    ) {
        super(strategoRuntimeProvider, astSupplier);
    }

    @Override public String getId() {
        return "{{genIndexAstTaskDef.qualifiedId}}";
    }
}
