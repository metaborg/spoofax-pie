package {{baseLanguageFactory.packageId}};

import org.checkerframework.checker.nullness.qual.Nullable;
import org.eclipse.core.runtime.IExecutableExtensionFactory;

public class {{baseLanguageFactory.id}} implements IExecutableExtensionFactory {
    private static {{language.nullableQualifiedId}} instance;

    public static {{language.qualifiedId}} getLanguage() {
        if(instance == null) {
            instance = new {{language.qualifiedId}}();
        }
        return instance;
    }

    @Override public {{language.qualifiedId}} create() {
        return getLanguage();
    }
}
