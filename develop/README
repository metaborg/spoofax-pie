# Spoofax 3 Documentation
To auto-build and view the documentation locally at <http://localhost:8000>,
invoke:

```bash
make
```

To deploy the documentation to GitHub pages at <https://metaborg.github.io/spoofax-pie/>, invoke:

```bash
make deploy
```
