/* factorial function */
let
  function fact(n : int) : int =
      if n < 1 then 1 else (n * fact(n - 1))
in let
  var x : int := 3
in
    fact(x)
//    let
//      var n : int := x
//    in
//      if n < 1 then 1 else (n * fact(n - 1))
//    end
end end

