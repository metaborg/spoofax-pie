package mb.statix;

import mb.resource.hierarchical.ResourcePath;

public class StatixConfig {
    public static StatixConfig createDefault(ResourcePath root) {
        return new StatixConfig();
    }
}
