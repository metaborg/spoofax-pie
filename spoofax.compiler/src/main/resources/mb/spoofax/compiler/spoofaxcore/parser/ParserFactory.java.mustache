package {{languageProject.packageId}};

import mb.spoofax.compiler.interfaces.spoofaxcore.ParserFactory;

public class {{genParserFactoryClass}} implements ParserFactory {
    private final {{genParseTableClass}} parseTable;

    public {{genParserFactoryClass}}() {
        this.parseTable = {{genParseTableClass}}.fromClassLoaderResources();
    }

    @Override public {{genParserClass}} create() {
        return new {{genParserClass}}(parseTable);
    }
}
