plugins {
  id("org.metaborg.gradle.config.root-project") version "{{shared.metaborgGradleConfigVersion}}"
}

subprojects {
  metaborg {
    configureSubProject()
  }
}

allprojects {
  repositories {
  {{#repositoryCodes}}
    {{this}}
  {{/repositoryCodes}}
  }
}
