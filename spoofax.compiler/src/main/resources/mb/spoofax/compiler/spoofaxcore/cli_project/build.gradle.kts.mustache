plugins {
  id("org.metaborg.gradle.config.java-application")
}

group = "{{shared.cliProject.coordinate.groupId}}"
version = "{{shared.cliProject.coordinate.version}}"

application {
  mainClassName = "{{main.qualifiedId}}"
}

sourceSets {
  main {
    java {
      srcDir("$buildDir/generated/sources/spoofax/java")
    }
    resources {
      srcDir("$buildDir/generated/sources/spoofax/resources")
    }
  }
}

dependencies {
{{#dependencyCodes}}
  {{this}}
{{/dependencyCodes}}
}
