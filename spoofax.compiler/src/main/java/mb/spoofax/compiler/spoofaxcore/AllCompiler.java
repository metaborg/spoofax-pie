package mb.spoofax.compiler.spoofaxcore;

import mb.resource.hierarchical.HierarchicalResource;
import mb.spoofax.compiler.util.ResourceDependencies;

import java.io.IOException;
import java.nio.charset.Charset;

public class AllCompiler {
    public ResourceDependencies compile(ParserCompilerInput input, HierarchicalResource langDir, Charset charset) throws IOException {
        return null;
    }
}
